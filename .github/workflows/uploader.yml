name: Uploader

on:
  pull_request:
    paths:
      - "**/*.go"
      - "go.mod"
      - "go.sum"
      - "Taskfile.yml"
      - ".golangci.yml"
      - ".golangci-lint-version"
      - ".zig-version"
      - ".github/workflows/uploader.yml"
  push:
    branches:
      - main
    paths:
      - "**/*.go"
      - "go.mod"
      - "go.sum"
      - "Taskfile.yml"
      - ".golangci.yml"
      - ".golangci-lint-version"
      - ".zig-version"
      - ".github/workflows/uploader.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lint:
    name: Uploader lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: .
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Task
        uses: go-task/setup-task@v1

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25"

      - name: Install Linux GUI build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends libgl1-mesa-dev xorg-dev

      - name: Restore Go and lint caches
        uses: actions/cache@v4
        with:
          path: |
            ~/go/pkg/mod
            ${{ github.workspace }}/.tmp/go-build-cache
            ${{ github.workspace }}/.tmp/golangci-lint-cache
          key: ${{ runner.os }}-go-uploader-${{ hashFiles('go.sum', '.golangci-lint-version') }}
          restore-keys: |
            ${{ runner.os }}-go-uploader-

      - name: Install golangci-lint
        run: |
          VERSION="$(cat .golangci-lint-version)"
          curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/HEAD/install.sh | sh -s -- -b /usr/local/bin "$VERSION"

      - name: Lint uploader
        run: task lint:go

  test:
    name: Uploader test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: .
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Task
        uses: go-task/setup-task@v1

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25"

      - name: Install Linux GUI build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends libgl1-mesa-dev xorg-dev

      - name: Restore Go cache
        uses: actions/cache@v4
        with:
          path: |
            ~/go/pkg/mod
            ${{ github.workspace }}/.tmp/go-build-cache
            ${{ github.workspace }}/.tmp/golangci-lint-cache
          key: ${{ runner.os }}-go-uploader-${{ hashFiles('go.sum', '.golangci-lint-version') }}
          restore-keys: |
            ${{ runner.os }}-go-uploader-

      - name: Test uploader
        run: task test:go
